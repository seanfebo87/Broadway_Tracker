<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Golf Tracker</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.1/css/bulma.min.css">
    <script defer src="https://use.fontawesome.com/releases/v5.0.7/js/all.js"></script>
</head>
<body>
<% if flash.has?(:message) %>
  <div class="notification is-danger has-text-weight-bold">
  <%= flash[:message] %>
  </div>
<% end %>
    <h1 class="has-text-weight-bold">Edit Your Record.</h1>
    <br>
    <form action='/posts/<%=@post.id%>/edit' method='post'>
        <div class="field">
            <p class="control">
                <input class="input" type="date" name="date" value="<%=@post.date%>">
            </p>
        </div>
        <div class="field">
            <p class="control">
                <input class="input" type="text" name="course" value="<%=@post.course%>">
            </p>
        </div>
        <div class="field">
            <p class="control">
                <input class="input" type="text" name= "score" value="<%=@post.score%>">
            </p>
        </div>
        <div class="field">
            <p class="control">
                <button class="button is-success">
                Submit
                </button>
            </p>
        </div>
    </form>
</body>
</html>
