<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Golf Tracker</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.1/css/bulma.min.css">
    <script defer src="https://use.fontawesome.com/releases/v5.0.7/js/all.js"></script>
</head>
<body>
    <section class="hero is-light is-fullheight">
    <div class="hero-head">
    <h1 class="has-text-weight-bold has-text-link is-capitalized title"><%=@user.username%>'s golf tracker</h1>
    <br>
    <table class="table is-hoverable is-fullwidth has-background-light">
        <thead>
            <tr>
            <th>Date</th>
            <th>Course</th>
            <th>Score</th>
            <th></th>
            </tr>
        </thead>
        <tbody>
            <%@user.posts.each do |post|%>
            <tr>
            <td><%=post.date%></td>
            <td><%=post.course%></td>
            <td><%=post.score%></td>
            <td>
            <div class="field is-grouped">
            <form action="/posts/<%=post.id%>/edit">
            <input type="submit" value="Edit">
            </form>
            <form action="/posts/<%=post.id%>/delete">
            <input type="submit" value="Delete">
            </form>
            </div>
            </td>
            </tr>
            <%end%>
        </tbody>
    </table>
    </div>
  <div class="field is-grouped">
  <form action='/posts/<%=@user.id%>/new'>
    <button class="button is-success">
      Add New Record
    </button>
  </form>
  <form action='/logout' method='post'>
    <button class="button is-danger">
      Logout
    </button>
  </form>
  </div>
  </section>
</body>
</html>
